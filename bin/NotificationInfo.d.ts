export declare class NotificationInfo {
    identifier: string;
    object?: any;
    /**
     * Creates a `NotificationInfo`
     *
     * @param identifier - identifier of the notification
     * @param object - object broadcasted with the notification
     */
    constructor(identifier: string, object?: any);
}
